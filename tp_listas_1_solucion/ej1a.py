# Escriba un programa que imprima todos los elementos de la lista y la longitud total de la lista ```conex```

conexiones = ["127.0.0.1", "root", "123456", "nomina"]

# Imprimir cada elemento
for elemento in conexiones:
  print(elemento)

# Imprimir longitud de la lista
print(len(conexiones))
